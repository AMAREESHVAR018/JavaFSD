# Exercise 41 - Executor Service and Callable

## Objective
Run a task with ExecutorService, Callable, and Future.

## File Structure
- `ExecutorCallableExample.java`

## Sample Output
```text
Total registrations: 3
```

## Explanation
A `Callable` returns a value that is retrieved through a `Future`.

## Run Commands
```bash
javac -d out ExecutorCallableExample.java
java -cp out com.community.concurrent.ExecutorCallableExample
```
